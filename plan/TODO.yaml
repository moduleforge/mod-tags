slug: tags-apiresp-migration
version: 1
title: ""
phases:
    - number: 1
      slug: go-apiresp-migration
      title: Go Apiresp Migration
      diff_base: 05617a895ad0f1ceddbe30501aa0e8bcfdacf3e9
      tasks:
        - number: 1
          slug: response-layer-migration
          title: Response Layer Migration
          path: phase-01-go-apiresp-migration/001-response-layer-migration.md
          done: true
          tier: sonnet-high
          digest: 'Migrated mod-tags'' api/ layer onto the shared Wave-0 apiresp package: re-homed service sentinels as aliases, converted all handler success/error writes to apiresp.WriteJSON/WriteError with canonical codes (unauthenticated/invalid_input), and added an end-to-end test proving masked GET /tags/{uuid} misses now return 403 (nested envelope), fixing the latent entity.ErrForbidden→500 bug. All validation checks (build, test, grep, manual scan) pass.'
          git:
            branch: phase-01-task-01-response-layer-migration
            commit:
                - 9ecc67d
            merge: b9f7fd7362e8788e02349e2e115a7431389af0cb
        - number: 2
          slug: entityresolver-masking-adoption
          title: Entityresolver Masking Adoption
          path: phase-01-go-apiresp-migration/002-entityresolver-masking-adoption.md
          done: true
          tier: sonnet-high
          digest: 'Extended mod-tags'' TagService masking coverage from the single already-migrated GetByUUID site to all remaining external-UUID lookups in tag.go: Create and ListBySubject''s subject-entity resolutions, UpdateByUUID/UpdateTagValue/DeleteByUUID''s pre-tx tag-entity resolutions (with residual in-tx GetTagByEntityUUID misses also masked to ErrForbidden), and Search''s owner/subject filter misses (now 403 instead of empty result, a deliberate flagged behaviour change). ListBySubject''s conversion directly resolves the open ''verify list-route masking consistency'' followup: the subject/parent entity is resolved via entityResolver.Resolve before authorization, so an inaccessible subject masks to 403 exactly like a direct lookup — this followup can be considered resolved. Updated six service tests and four handler tests, added two new service tests. All validation (build, vet, test, gofmt, grep) passes green.'
          git:
            branch: phase-01-task-02-entityresolver-masking-adoptio
            commit:
                - d875cce
            merge: 35be9f8c0bb9ae4e06eeedf42ead591b23f3fa7f
    - number: 2
      slug: gui-error-handling
      title: GUI Error Handling
      diff_base: 72002f9aaaa6e2e669777c1ab6efe894b3e3e46d
      tasks:
        - number: 1
          slug: adopt-shared-request-client
          title: Adopt Shared Request Client
          path: phase-02-gui-error-handling/001-adopt-shared-request-client.md
          done: true
          tier: sonnet-high
          digest: Rewrote gui/src/lib/api.ts to route all five createTagsClient methods through @moduleforge/core-gui's shared request() wrapper, removing the bespoke handleResponse/ErrorResponse/throw-new-Error transport layer. Preserved baseUrl composition, headers() injection hook, purpose-filtering logic, JSON bodies, and .tags extraction. Re-exported (not redefined) ApiError/ApiErrorResponse/FieldErrorData/ApiRequestError from core-gui. Promoted core-gui from optional to required peer dependency, adding an overrides entry so bun install resolves it via the local yalc store. typecheck and build both green; public surface (createTagsClient signatures, Tag/TagsClientOptions, index.ts exports) unchanged.
          git:
            branch: phase-02-task-01-adopt-shared-request-client
            commit:
                - 15e94e4
            merge: 857833a228e7406b72b133e6940557a8f983e7f1
        - number: 2
          slug: migrate-error-widgets
          title: Migrate Error Widgets
          path: phase-02-gui-error-handling/002-migrate-error-widgets.md
          done: false
          tier: sonnet-high
          digest: Migrated all seven ad-hoc err instanceof Error catch sites across TagEditor.tsx (six) and TagList.tsx (one) onto core-gui's useApiError hook, rendering field-bound invalid_input details via FieldError and banner-level errors via ErrorBanner, with toast-worthy classes auto-dispatched to the Toast provider. Local fetchError/submitError/errorMessage string state replaced with typed ApiRequestError|null state; raw role="alert" markup removed entirely. Fixed a latent bug where TagEditor's mutation-error banner could never display. mockClient.ts now throws representative ApiRequestErrors; both story files gained new field/banner/toast stories wrapped in ToastProvider. Forwarded ApiRequestError/ApiError/ApiErrorResponse/FieldErrorData from gui/src/index.ts, closing a gap task 001 flagged. typecheck, build, both required greps, and static+dev Ladle build/serve all passed.
          git:
            branch: phase-02-task-02-migrate-error-widgets
            commit:
                - ad05d99
            merge: null
    - number: 3
      slug: doc-updates
      title: Documentation Updates
      tasks:
        - number: 1
          slug: update-architecture-docs
          title: Update Architecture Docs
          path: phase-03-doc-updates/001-update-architecture-docs.md
          done: false
          tier: sonnet-high
          git:
            branch: null
            commit: []
            merge: null
