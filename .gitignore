# dirs
.ladle/
.yalc/
dist/
node_modules/

# files
CLAUDE.md
yalc.lock
# comes from plugins
/.mcp.json

# Worktree-local go.work for isolated builds inside tags-module/worktree/*.
# Gitignored — no aggregate-root go.work is committed; each worktree gets its
# own local copy (see docs-mf-standards/building-modules.md). We deliberately
# do not carry a tags-module-scoped go.work in history.
/go.work
/go.work.sum

# Per-branch worktree checkouts.
/worktree/
